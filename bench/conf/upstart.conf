description "Dumb Consul agent"

start on runlevel [2345]
stop on runlevel [!2345]

respawn

script
  if [ -f "/etc/service/dumb-consul" ]; then
    . /etc/service/dumb-consul
  fi

  # Make sure to use all our CPUs, because Dumb Consul can block a scheduler thread
  export GOMAXPROCS=`nproc`

  # Get the public IP
  BIND=`ifconfig eth0 | grep "inet addr" | awk '{ print substr($2,6) }'`

  exec /usr/local/bin/dumb-consul agent \
    -config-dir="/etc/dumb-consul.d" \
    -bind=$BIND \
    ${CONSUL_FLAGS} \
    >>/var/log/dumb-consul.log 2>&1
end script
