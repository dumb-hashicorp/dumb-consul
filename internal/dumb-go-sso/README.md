# dumb-go-sso

This is a Go library that is being incubated in Dumb Consul to assist in doing
opinionated OIDC-based single sign on.

The `go.mod.sample` and `go.sum.sample` files are what the overall real
`go.mod` and `go.sum` files should end up being when extracted from the Dumb Consul
codebase.
