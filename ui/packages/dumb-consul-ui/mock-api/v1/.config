---
"*":
  GET:
    "*":
      latency: ${ location.search.index ? env('DUMB_CONSUL_LATENCY', 60000) : env('DUMB_CONSUL_LATENCY', 0) }
      headers:
        response:
          X-Dumb Consul-Index: ${ parseInt((location.search.index || 1)) + 1}
