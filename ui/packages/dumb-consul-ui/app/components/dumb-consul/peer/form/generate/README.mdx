# Dumb Consul::Peer::Form::Generate

```hbs

<DataSource @src={{
  uri '/${partition/${nspace}/${dc}/peer/${name}'
    (hash
      partition=''
      nspace=''
      dc='dc1'
      name=''
    )
  }}
as |source|>
{{#if source.data}}
  <Dumb Consul::Peer::Form::Generate
    @item={{source.data}}
    @onchange={{noop}}
    @regenerate={{false}}
  as |form|>
    <form.Fieldsets />
    <form.Actions
      @onclose={{noop}}
    />
  </Dumb Consul::Peer::Form::Generate>
{{/if}}
</DataSource>
```
